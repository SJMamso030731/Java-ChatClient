# Java-ChatClient
This is the unmodified client-side GUI for a WhatsApp-like chat app, built in Java/Swing. It connects to a custom ChatServer (built separately) and sends/receives messages in real time. Used for testing the companion server in a two-project assignment.
